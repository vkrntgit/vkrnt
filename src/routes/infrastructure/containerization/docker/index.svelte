<h2> Docker </h2>
<br>
<br>
<h3> Common docker commands </h3> 
docker container start <br>
Docker container stop <br>
Docker container run <br>
Cmd + p + <br>
docker compose -d <br>
docker image ls <br>
docker compose ls <br>
Docker network (creates a network- explore more) <br>
Docker service create <br>
Docker image build -t &lt; dockerhubid/repo:version &gt; <br>
Docker image push <br>
Docker image ls <br>
Docker container run -d --name &lt; name &gt; -p 8000:8080 <br>
Docker swarm init (form one node) <br>
Docker swarm join-token worker <br>
<br><br>  
